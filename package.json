{
  "name": "redux-demo",
  "version": "1.0.0",
  "description": "Redux Application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf build/ && babel ./src --out-dir build/ --copy-files",
    "start": "npm run build && node build/index.js"
  },
  "author": "Arun Subramonia Sarma",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "redux": "^4.0.0",
    "redux-actions": "^2.6.1",
    "redux-logger": "^3.0.6",
    "redux-promise": "^0.6.0",
    "redux-promise-middleware": "^5.1.1",
    "redux-thunk": "^2.3.0",
    "seamless-immutable": "^7.1.4"
  }
}
